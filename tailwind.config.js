const colors = require('tailwindcss/colors');

module.exports = {
	// ...existing config...
	theme: {
		extend: {
			colors: {
				sky: colors.sky,
			},
		},
	},
	plugins: [],
	// ...existing config...
};
